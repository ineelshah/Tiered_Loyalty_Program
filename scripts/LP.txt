CREATE TABLE RE_RULE
(
    RULEID VARCHAR2(255),
    POINTS VARCHAR2(255),
    VERSION NUMBER,
    PRIMARY KEY (RULEID, VERSION)
)

CREATE TABLE RR_RULE
(
    RULEID VARCHAR2(255),
    POINTS VARCHAR2(255),
    VERSION NUMBER,
    PRIMARY KEY (RULEID, VERSION)
)